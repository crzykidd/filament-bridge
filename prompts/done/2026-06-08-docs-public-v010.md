---
name: 2026-06-08-docs-public-v010
status: done
created: 2026-06-08
model: sonnet
completed: 2026-06-07
result: README overhauled (architecture fixed, quick-start volume added, ghcr.io/hyiger image, full env table, feature list, safety model, OpenTag section, prerequisites, local dev, screenshots placeholder); docs/configuration.md created (complete env reference, grouped, two-axis model documented); CHANGELOG [Unreleased] replaced with full Added/Changed/Fixed set.
---

# Task: v0.1.0 public docs — README overhaul + docs/configuration.md + CHANGELOG [Unreleased]

Documentation-only. Update the public-facing docs to be accurate for a v0.1.0 release. ONE
`docs:` commit. Touch ONLY these files (+ the prompt move): `README.md`,
`docs/configuration.md` (new), `CHANGELOG.md`. Do NOT edit `docs/prd.md`,
`docs/spoolman-writes.md`, `CLAUDE.md`, or `docs/decisions.md` (a separate task owns those).

**Source of truth:** read `backend/app/config.py` for the authoritative env-var list/defaults,
`docker-compose.yml` + `Dockerfile` for the deploy shape, and `frontend/src/pages/` +
`backend/app/api/` for the feature set. Verify before writing — don't trust this prompt's
summary blindly.

## A. README.md overhaul

Fix the actively-wrong items and bring it current:
1. **Quick-start `docker-compose.yml` MUST include the SQLite volume** — `bridge-data:/data`
   (the `DATA_DIR` default; without it ALL bridge state is lost on restart). Mirror the real
   `docker-compose.yml` volume. This is a correctness fix, not cosmetic.
2. Replace the placeholder image `ghcr.io/yourname/filament-bridge` with `ghcr.io/hyiger/filament-bridge`
   (match the other images' org). Add a one-line pointer that the repo ships a full runnable
   stack at `docker-compose.yml`.
3. **Fix the architecture diagram**: the bridge talks ONLY to the Spoolman API and Filament DB
   API. OctoPrint/Moonraker/Klipper sit BEHIND Spoolman (upstream of it), not as bridge peers.
4. **Env table**: document ALL settings from `config.py` (currently ~8 are missing: `DATA_DIR`,
   `VARIANT_LINE_KEYWORDS`, `SPOOLMAN_FIELD_FILAMENTDB_MATERIAL_TAGS`, `MATERIAL_TAG_IDS`,
   `OPENTAG_VENDOR_ALIASES`, `SPOOLMAN_FIELD_OPENPRINTTAG_SLUG`,
   `SPOOLMAN_FIELD_OPENPRINTTAG_UUID`, `OPENTAG_CACHE_MAX_AGE_HOURS`). Keep the full table OR
   keep a short "common vars" table and link to the new `docs/configuration.md` for the
   complete reference — but make the link valid (the current `docs/configuration.md` link is
   dead).
5. **Rewrite the feature list** to cover the shipped product: bidirectional sync engine; guided
   initial-sync wizard with fuzzy match review (group/sort/filter, bulk, rescan); usage-logged
   weight sync (net↔gross, decrements → FDB usage entries); field + cost mapping;
   Spoolman→FDB variant grouping; structured multicolor/gradient sync (FDB ≥1.33.0);
   material-finish tag round-trip; conflict queue (manual resolution); upstream-deletion
   detection; **OpenTag (OpenPrintTag) cleanup tool**; runtime **Settings** (per-category sync
   direction + conflict policy). Don't overclaim Discord (env var only, not implemented).
6. **Add a "Safety model / what the bridge will never do" section** (these are the selling
   points): auto-sync OFF by default + explicit enable; conflicts never auto-resolved; never
   deletes upstream records without explicit user action; weight decrements logged as usage
   (audit trail preserved); no upstream code modification (REST APIs + Spoolman extra fields
   only).
7. **Add an "OpenTag cleanup tool" section** (brief): matches Spoolman filaments against the
   OpenPrintTag database, per-field review + candidate picker + vendor aliases, applies to
   Spoolman and stamps the OpenPrintTag slug/uuid into FDB. Note routes are `/api/openprinttag/*`
   (renamed off `opentag` to dodge ad-blockers).
8. **Add "Prerequisites"**: Filament DB (multicolor needs ≥1.33.0) + Spoolman, both APIs
   unauthenticated; Spoolman extra fields are auto-created by the bridge on startup.
9. **Add "Local development"**: backend `uvicorn app.main:app --reload --port 8090`, frontend
   `npm run dev`, required env vars, tests (`pytest`, `npm test`) — mirror CLAUDE.md's dev flow.
10. **Screenshots**: leave a `## Screenshots` section with TODO placeholders (e.g. HTML
    comments listing dashboard/wizard/conflicts/OpenTag) — do NOT fabricate images. (User adds
    later.)
11. Keep the existing "Why?" intent. Keep the CHANGELOG link.

Aim for a clean, well-structured README a new operator can actually follow. Use the
"Recommended structure": title/desc → Why → What it does → Screenshots(placeholder) → Safety
model → Architecture → Quick start (Docker) → Prerequisites → First run (wizard) →
Configuration → How sync works → OpenTag cleanup → Local development → Changelog → Contributing
→ License.

## B. docs/configuration.md (new)

Create the complete configuration reference the README links to: every env var from
`config.py` with Required/Default/Description, grouped (Core/Connection, Sync behavior,
Cross-reference fields, Field mapping, Variant grouping, Material tags + OpenTag, Logging/Data).
Note which settings are ALSO runtime-editable in the Settings UI (sync direction + conflict
policy per category, variant keywords, vendor aliases). Document the two-axis model briefly
(direction × conflict_policy per category; `newest_wins` is weight-only).

## C. CHANGELOG.md — populate [Unreleased]

The current `[Unreleased]` is stale (predates ~40 commits, still references the removed
source-of-truth model). REPLACE the `[Unreleased]` body with a complete Added/Changed/Fixed
set covering the work since the project start. Use `git log --since="2026-06-05" --pretty="%h %s"`
to ground it, and collapse the many incremental OpenTag-cleanup commits into a coherent feature
summary (don't list each commit). Keep it under `## [Unreleased]` — do NOT roll to `[0.1.0]`
or add a date (the `/release-prep` step owns the version roll). Cover at minimum: the wizard,
variant grouping, sync engine, per-category direction+conflict policy, cost sync, multicolor
sync, variances step, deletion detection, the OpenTag cleanup tool, finish-tag round-trip,
location carry-over, the web UI, CI/release tooling (Added); the source-of-truth→two-axis
change, enforced new-spool direction, material_tags CSV storage, opentag route rename
(Changed); and the spool-xref, tare/netFilamentWeight, multicolor both-colors-422,
extra-field self-heal, and matcher-correctness fixes (Fixed). A detailed draft exists in the
audit — you may adapt it, but verify against the git log.

## Verification

- No code touched — nothing to test. Run `git diff --stat` to confirm only the three intended
  files (+ prompt move) changed.
- Sanity: the README quick-start compose block has the `/data` volume; the env table matches
  `config.py`; the `docs/configuration.md` link resolves.

## When done

1. Frontmatter; `git mv` this prompt to `prompts/done/`.
2. Stage ONLY `README.md docs/configuration.md CHANGELOG.md` + the moved prompt; commit on
   `dev` with one `docs:` message (e.g. `docs: v0.1.0 README overhaul, configuration reference, changelog`).
   Use a pathspec-scoped commit (commit only those paths). If `git commit` hits an index lock,
   wait ~5s and retry once. Never `git add -A`. Never push.
