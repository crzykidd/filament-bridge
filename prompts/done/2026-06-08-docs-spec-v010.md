---
name: 2026-06-08-docs-spec-v010
status: done
created: 2026-06-08
model: sonnet
completed: 2026-06-07
result: docs updated — PRD FR-2/8/13/16/20-25 + OpenTag FR + structure block; spoolman-writes name/vendor/multi_color_direction + never-both note; CLAUDE.md 7 new env rows
---

# Task: v0.1.0 spec/reference docs — PRD update + spoolman-writes.md + CLAUDE.md env table

Documentation-only. Make the spec and internal reference docs accurate for v0.1.0. ONE `docs:`
commit. Touch ONLY these files (+ the prompt move): `docs/prd.md`, `docs/spoolman-writes.md`,
`CLAUDE.md`. Do NOT edit `README.md`, `docs/configuration.md`, or `CHANGELOG.md` (a separate
task owns those). Don't add `docs/decisions.md` entries (it's already current).

**Source of truth:** verify everything against the code before writing — `backend/app/core/`
(`sync_policy.py`, `engine.py`, `weight.py`, `fields.py`, `color.py`, `material_tags.py`,
`opentag_match.py`), `backend/app/api/` (`opentag.py`, `conflicts.py`, `config.py`,
`backup.py`), `backend/app/config.py`, and `backend/app/models/`.

## A. docs/prd.md — make it match the implementation

Apply these edits (the behavior described is what the code actually does today):

1. **Rewrite FR-2 and FR-8 (source of truth → two-axis model).** Replace the single
   "source of truth per category" with: each category (weight, material_properties, new_spool)
   has a `direction ∈ {two_way, spoolman_to_filamentdb, filamentdb_to_spoolman}` and a
   `conflict_policy ∈ {manual, spoolman_wins, filamentdb_wins, newest_wins}`, resolved by
   `core/sync_policy.py:resolve_sync_action`. Note `newest_wins` is WEIGHT-ONLY (rejected for
   material_properties in `api/config.py`). new_spool uses a direction only. Retire the
   `*_source_of_truth` language.
2. **Rewrite FR-13 (conflicts)** to enumerate the three origins: both-sides-changed under a
   manual two-way policy; AND a new **upstream-deletion** conflict class
   (`DELETION_FIELD = "__record_deleted__"`, `engine.py:_queue_deletion_conflict`,
   `models/conflict.py`, cleanup in `api/conflicts.py`). Add a short FR or subsection for
   deletion detection.
3. **Fix FR-16 resolution semantics**: resolving a conflict RECORDS the chosen value and
   removes it from the open queue — it does NOT write upstream (apply-on-next-cycle is a
   Phase-2 follow-up). Match `api/conflicts.py` ("performs no upstream writes").
4. **Add an FR section for the OpenTag (OpenPrintTag) Cleanup tool**: matches Spoolman
   filaments against the OpenPrintTag dataset (cached from FDB's `/api/openprinttag`, secondary
   colors recovered from the raw tarball), per-field review with a best+top-5 candidate picker,
   vendor→brand aliases, a reviewable Manufacturer field (vendor find-or-create reassignment),
   UUID exact-match, applies to Spoolman + stamps `openprinttag_slug`/`uuid` into FDB's
   `settings{}` bag (the approved scoped-exception path). Routes are `/api/openprinttag/*`.
5. **Add FRs / data-flow entries for the other shipped engine passes**: cost sync
   (spool-price-first, bidirectional; FDB→SM writes filament price only); structured
   multicolor/gradient sync (version-gated FDB ≥1.33.0; never both color_hex+multi_color_hexes);
   material-finish tag round-trip (`optTags` ↔ `filamentdb_material_tags`). Update the
   data-flow diagram to include `_sync_cost`, `_sync_multicolor`, `_sync_finish_tags`,
   `_sync_opentag_identity`.
6. **Correct FR-24 backup routes** to `GET /api/backup/export` and `POST /api/backup/import`.
   Mark **FR-25 (config-only export)** Not-implemented/deferred (folded into full backup).
7. **Mark FR-20 (Discord) and FR-22 (print-history) as Not-implemented for v0.1.0** — only the
   `DISCORD_WEBHOOK_URL` env var is declared; no posting/print-history code exists. Mark
   **FR-21 (archive/retire) Partial** — archived Spoolman spools are detected/handled but
   there's no symmetric `retired↔archived` propagation.
8. **Fix deep-link description**: the UI renders text badges "FDB" (blue) / "SM" (emerald),
   not vendor logos. URL patterns are unchanged/correct.
9. **Refresh the project-structure/architecture block** to list the new modules: `core/`
   {`sync_policy`,`planner`,`dryrun`,`color`,`material_tags`,`version`,`opentag_match`,
   `opentag_cache`,`opentag_secondary`}, `api/opentag.py`, and frontend `Settings.tsx` +
   `OpenTagCleanup.tsx`.
10. **Open questions**: note post-2026-05-28 decisions and add "resolved by code" pointers for
    weight-threshold, extra-field creation, inherited-variant handling, full-snapshot diff, and
    deep-link URL patterns. Keep OctoPrint-2.0 / Moonraker deferred.

Preserve the PRD's overall structure/voice; surgically rewrite the affected sections rather
than rewriting the whole file. Keep P0/P1/P2 priorities.

## B. docs/spoolman-writes.md — add the missing OpenTag-apply writes

Cross-check the OpenTag "Apply" section against `opt_to_spoolman_fields`
(`core/opentag_match.py`) + `_build_sm_patch`/`opentag_apply` (`api/opentag.py`). Add the
fields the table currently omits:
- **`name`** (native) — the reviewable name field (default OpenTag).
- **`vendor` → `vendor_id`** — the Manufacturer field reassigns the Spoolman vendor via
  find-or-create (`_ensure_vendor`: `get_vendors` + `create_vendor`); document that this is the
  one OpenTag path that may CREATE a Spoolman vendor.
- **`multi_color_direction`** (written together with `multi_color_hexes`).
Add a clear note in the "what the bridge never writes / notes" area: the bridge NEVER sets both
`color_hex` and `multi_color_hexes` on the same filament (Spoolman 422s) — multicolor uses
`multi_color_hexes` + `multi_color_direction` only. Keep the rest (already accurate).

## C. CLAUDE.md — complete the env-var table

The env table in CLAUDE.md is missing several settings present in `config.py`. Add the missing
rows (verify names/defaults against `config.py`): `DATA_DIR`,
`SPOOLMAN_FIELD_FILAMENTDB_MATERIAL_TAGS`, `MATERIAL_TAG_IDS`, `OPENTAG_VENDOR_ALIASES`,
`SPOOLMAN_FIELD_OPENPRINTTAG_SLUG`, `SPOOLMAN_FIELD_OPENPRINTTAG_UUID`,
`OPENTAG_CACHE_MAX_AGE_HOURS` (and confirm `VARIANT_LINE_KEYWORDS` is present). Only touch the
env table (and adjacent prose if a var needs a one-line note) — do not restructure CLAUDE.md.

## Verification

- No code touched. Run `git diff --stat` — only `docs/prd.md`, `docs/spoolman-writes.md`,
  `CLAUDE.md` (+ prompt move) should change.
- Spot-check: PRD FR-2/8 describe direction+policy; FR-16 says record-not-write; FR-20/22 marked
  not-implemented; backup routes corrected; spoolman-writes lists name/vendor/multi_color_direction
  + the never-both note; CLAUDE.md env table includes the added vars.

## When done

1. Frontmatter; `git mv` this prompt to `prompts/done/`.
2. Stage ONLY `docs/prd.md docs/spoolman-writes.md CLAUDE.md` + the moved prompt; commit on
   `dev` with one `docs:` message (e.g. `docs: align PRD + spoolman-writes + CLAUDE.md with v0.1.0 implementation`).
   Use a pathspec-scoped commit. If `git commit` hits an index lock, wait ~5s and retry once.
   Never `git add -A`. Never push.
