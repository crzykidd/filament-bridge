# vexp-context-engine standard (Shape B): the vexp index is NOT tracked — neither
# index.db nor manifest.json. Each host builds and owns its own index. Nothing in
# .vexp/ is version-controlled except this file and .gitignore.
