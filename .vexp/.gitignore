# vexp — internal files (auto-generated, machine-local, not tracked).
# Per the vexp-context-engine standard (Shape B): each host builds and owns its own
# index, so the manifest is NOT tracked either — nothing here is kept fresh in git.
index.db
index.db-wal
index.db-shm
manifest.json
vexp.log
daemon.log
daemon.sock
daemon.pid
daemon.pipe
mcp.port
healthy
parent_workspace.json
workspace.json
token-savings-report.md
